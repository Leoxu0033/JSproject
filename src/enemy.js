// Re-export the clean Enemy implementation kept in `enemy2.js`.
// This keeps existing imports stable while the clean implementation
// lives in `src/enemy2.js`.
export { Enemy } from './enemy2.js';
